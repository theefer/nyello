
void
loop(char* argv[], int argc) {
  int argIndex = 0;
  int state = START;
  int next_state;

  if(argc == 0) {
    // SELECT <current playing song>
  }

  while(argIndex < argc) {
    switch(state) {
    case START:

      next_state = 

      break;

      case 
    }
  }

}
